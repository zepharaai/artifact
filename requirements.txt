# Python 3.8–3.10 recommended
# Install torch CPU wheel first using the PyTorch CPU index (see README):
#   pip install torch==2.2.1 --index-url https://download.pytorch.org/whl/cpu

transformers==4.46.3
datasets==3.1.0
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.2
tqdm==4.67.1
# torch is installed separately to ensure the correct CPU wheel is chosen
